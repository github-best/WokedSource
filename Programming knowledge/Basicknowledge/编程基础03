
1.在同源文件下，html标签中的id名称，不能相同，否则浏览器不予以解析。

2.java，js中的日期对象，new Date();得到的时间为：本地当前时间；js中new Date(String format); 可以把字符串转为一个日期对象。

3.div、h1 或 p 元素常常被称为块级元素。这意味着这些元素显示为一块内容，即“块框”。与之相反，span 和 strong 等元素称为“行内元素”，
  这是因为它们的内容显示在行中，即“行内框”。
 
4.关于计算机中id的理解，id是identity(身份)的缩写，计算机程序是模拟现实生活的，id就像一个人的身份证号一样，
  尽管一个国家的人很多，但id是不重复的，所以在编程中，无论是前端页面中标签属性的id，还是数据库表字段主键的id，id值是不可能重复的，
  但是其他属性或数据库其他字段的值，是可以重复的。
  

 

