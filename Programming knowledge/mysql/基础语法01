1.mysql数据库索引类型：
  1.primary key-----------------------> 主键索引
  2.unique ---------------------------> 唯一索引
  3.index ----------------------------> 普通索引
  4.fulltext--------------------------> 全文索引
  
2.组合索引：
  即在索引函数中，写入多个字段，如： primary key (id,email,sno)
  1.主键组合索引，称之为------> 联合主键。
  2.唯一组合索引，数据入库时，只要有一个字段不同，即可入库成功。
  3.普通组合索引。
  注意：组合索引字段必须是连续的，否则索引失效。
  
3.数据库字段约束，字段加备注信息：
  1.字段约束：not null(字段值，不能为空)，
    
  
 

  
  
